# いまの状態と次の一手

セッションをまたいでも迷わないための現在地メモ。**作業したらここを更新する。**

最終更新:2026年8月29日

---

## 決まっていること

| 項目 | 値 |
|---|---|
| **屋号** | **`Bridge AI`**(確定。開業届・D-U-N-S・Apple・銀行すべてこの表記で統一する) |
| ドメイン | `bridge-ai-tokyo.com` |
| このアプリの公開URL | `https://toeic.bridge-ai-tokyo.com/` |
| プライバシーポリシー(App Storeに登録) | `https://bridge-ai-tokyo.com/privacy.html` |
| サポートURL(App Storeに登録) | `https://bridge-ai-tokyo.com/support.html` |
| 連絡先 | `contact@bridge-ai-tokyo.com` |
| バンドルID | `com.bridgeaitokyo.toeic` — **App ID 登録済み**(Description: `TOEIC Workbook`) |
| SKU | `toeic-workbook-001` — 非公開。**削除しても再利用できない** |
| 課金アイテムID | `com.bridgeaitokyo.toeic.unlockall` |
| アプリ名 | `TOEIC® 総合問題集` — 情報を足さない方針(問題数・セット数は説明文で伝える) |
| 価格 | アプリ本体=無料 / 全セット解放=**¥680**(非消費型・買い切り)。Web版(note)も同額 |
| 無料範囲 | セット1(200問)は全機能無料。セット2〜10が課金対象 |

**屋号は半角スペースありの `Bridge AI`。**開業届をこの表記で提出済みで、D-U-N-S と
Apple もこれに揃える。D&B と Apple の審査は申請名とサイト上の表記を機械的に照合する。
人間が見れば同じでも `BridgeAI` と `Bridge AI` は別文字列で、照合が取れないと
差し戻しになる。ハブサイトの「運営者情報」に正式表記を置いてあるのはこのため。

ドメインだけ `bridge-ai-tokyo.com` とハイフンなのは、ドメインに空白が使えないため。
こちらは揃える必要がない。

## サイトの構成

GitHub Pages は **1リポジトリ 1GB** が上限。この問題集だけで 290MB(音声 4,252 本で
142MB)使うので、同じリポジトリに製品を足すと 3 個目で頭を打つ。
そこで **製品1つ = リポジトリ1つ = サブドメイン1つ** に分けている。

| URL | リポジトリ | 中身 |
|---|---|---|
| `bridge-ai-tokyo.com` | [bridge-ai-tokyo](https://github.com/KazuhiroHashi/bridge-ai-tokyo) | 会社サイト(ハブ)。軽いまま保つ |
| `toeic.bridge-ai-tokyo.com` | toeic_test(これ) | TOEIC 問題集 |

ハブが軽いので、プライバシーポリシーの URL が動かない。App Store に登録するのは
ハブ側の `privacy.html`。**アプリにも同名のファイルを同梱している**(オフラインで
開けるようにするため)ので、内容を変えるときは両方直すこと。

## リリースの段取り(重要)

App Store の販売者名は、個人アカウントのままだと `Kazuhiro Hashino` になり、
**一度公開するとアプリ情報の収集サイトやアーカイブに永久に残る。**
プライバシーポリシーとサポートの URL は独自ドメインにしたので、残る露出はここだけ。

そこで **「審査まで通して、リリースだけ止める」** 進め方にする。

| 工程 | 公開されるか |
|---|---|
| App記録の作成・課金アイテム・ビルド・TestFlight・審査提出 | されない。**いま全部やってよい** |
| リリース | **される** ← 組織移行が終わるまで押さない |

審査提出時に「手動でリリース」を選べば、承認済みの状態で待機できる。
開業届 → D-U-N-S → 組織移行(1〜3か月)の待ち時間が、そのまま準備時間になる。

屋号については、`bridge-ai.jp` の会社と名称が重なるが、J-PlatPat で確認したところ
`Bridge AI` 単独の商標登録は存在しない。事業領域も違う(先方=BtoBのAI導入支援)。
将来AI事業に進出して規模が出たら、自分で出願することを検討する。

---

## 完成しているもの

- 問題 **2,000問**(全10セット × 200問、Part 1〜7、全問に解説・全訳)
- Part 1 の写真 60枚
- リスニング音声 4,252クリップ
- Web版・アプリ版のコード(アプリ内課金の実装を含む)

**中身の追加作業は残っていない。**残っているのは手続きと公開作業だけ。

---

## 販売の2チャネル

### A. Web版(GitHub Pages + note の合言葉)

**Appleと無関係に、いつでも売り始められる。**独自ドメインの設定が終われば公開できる。
集客は note の連載記事から。手順 → [tools/SELL_HOWTO.md](tools/SELL_HOWTO.md)

### B. iOSアプリ(App Store)

Apple 側の手続き待ち。手順 → [tools/IAP_HOWTO.md](tools/IAP_HOWTO.md)

---

## 次にやること(この順番)

### 1. ドメインの設定 — 済(残りは証明書待ちのみ)

- Cloudflare でドメイン取得。Whois 情報公開代行あり、自動更新オン(2027年8月8日まで)
- DNS:A 4つ(`@` → `185.199.108/109/110/111.153`)、CNAME 2つ(`www`・`toeic` → `kazuhirohashi.github.io`)。
  **すべて Proxy status は「DNS only」(灰色の雲)。** オレンジにすると GitHub が証明書を
  発行できずサイトが開かなくなるので、今後レコードを足すときも同じ
- 両リポジトリとも Pages の Custom domain 設定済み・DNS check successful
- Email Routing 有効。`contact@bridge-ai-tokyo.com` → 普段の Gmail に転送
- ハブサイトに **`support.html`** を新設し、屋号の表記を `Bridge AI` に統一。
  トップに「運営者情報」を置いてある(D&B と Apple が申請名とサイト表記を
  機械的に照合するため、正式表記を1行残す必要がある)

**残:`Enforce HTTPS` のチェック(証明書の発行待ち)。**
GitHub が Let's Encrypt を自動取得するので、発行され次第 2 リポジトリともチェックを入れる。
それまで `https://` は証明書エラーになる(`http://` なら開ける)。
24時間経っても発行されない場合は、Pages 設定で Custom domain を Remove → 入れ直すと再取得が走る。

`CNAME` は各リポジトリ直下に置いてある。**消さないこと。**

### 2. Web版の販売開始

note の第4回(AWS CLF)以降の執筆と並行して、合言葉の販売を始める。

### 3. Apple 側(1〜3か月かかる)

本名を App Store に残さないため、**組織アカウントへの移行を先に済ませてから**
App記録を作る。App記録を作った時点で販売者名が確定するため、順番を逆にできない。

1. 開業届を e-Tax で提出 — **済(屋号 `Bridge AI` で提出)**
   → **手順の詳細は [tools/YAGO_HOWTO.md](tools/YAGO_HOWTO.md)**
2. 勤務先に副業申請(年収見込みは「年間30万円程度」と控えめに)
3. D-U-N-S 番号を申請(約2週間。TSR から開業届の写しを求められる)
   → **別セッションで申請中。**事業者名は `Bridge AI`(半角スペースあり)
4. Apple に個人→組織の変更を申請(約2週間。フォームが法人前提でエラーになったら
   「個人事業主として組織アカウントに登録したい」とサポートに明示する)
5. 税務フォームの再提出(**最長2か月かかる報告あり。ここが最大の詰まり所**)
6. App記録を作成し、Developer名を `Bridge AI` に設定

### 4. ビルドと審査提出 — **1.0 公開済み / 1.0.1 審査中(2026年8月29日)**

- **1.0 は審査承認後にリリース済み**(販売者名は個人名のまま。組織移行が終わったら
  Developer 名が `Bridge AI` に切り替わる。移行は D-U-N-S 発行待ち)
- **1.0.1 を審査へ提出済み**(ビルド17)。内容:
  1. 解き方の切り替えを追加。既定は『1問ずつ答え合わせ』、もう一方は『本番と同じ』。
     設問が複数ある画面(Part 3/4/6/7)は全問答えてからまとめて答え合わせする
     (先に答えを見せると残りの設問のヒントになるため)。模試は設定に関係なく最後にまとめて採点
  2. セット7 Part3・セット7/9/10 Part4 の会話とトークを本番の長さ(150語以内)に短縮
- 審査を1度差し戻されている(Guideline 2.1 Information Needed)。**新規アプリの初回提出では
  画面録画と8項目の回答が必須。**返信文は tools/APPSTORE_HOWTO.md 手順13を参照

**バージョン番号は Run workflow の入力欄で指定する。**承認済みの番号と同じだと
アップロードが 90062 で弾かれる(ビルドと署名は成功していても失敗する)。

**音声を作り直したら 48kHz に揃えること。**同梱音声は全4,252クリップが
48kHz・モノラル・64kbps。edge-tts の既定は24kHzなので、生成しただけだと混在し、
その部分だけ画面収録で倍速になる。`ffmpeg -i x.mp3 -ar 48000 -ac 1 -b:a 64k out.mp3`。

初回ビルドが通り、署名済みの IPA が App Store Connect にアップロード済み。
以降の修正は「push → Actions で Run workflow → 5分待つ」だけで新しいビルドが
TestFlight に積まれる。

通すまでに直した4点(再発したらここを見る):
1. Node 20 → 22(Capacitor 8 の CLI が >=22 を要求)
2. workspace → xcodeproj(Capacitor 8 は SPM 構成で .xcworkspace が無い)
3. 自動署名 → fastlane の配布用手動署名(端末未登録だと開発用プロファイルが作れない)
4. macos-15 → macos-26(Apple が iOS 26 SDK 未満のビルドを受け付けない)

**証明書の使い回し(run #7 の上限エラーの再発防止):**
初めての実行で作った配布用証明書を p12 に書き出し、GitHub Actions の
キャッシュ(キー `dist-cert-v1`)へ保存して、次回からはそれを取り込む。
これで実行のたびに証明書が増えることはない。
それでも「maximum number of certificates」で落ちたら、developer.apple.com の
Certificates で Apple Distribution を**全部** Revoke してから再実行する
(CI が作った証明書の秘密鍵はランナーと一緒に消えているので、残しても使えない)。
失効はビルド済み・審査済みのアプリに影響しない。
キャッシュは最終利用から7日で自動削除されるが、消えても次の実行が
作り直して保存し直すだけ(そのとき上限に当たっていればまず Revoke)。

**Mac がなくても可能。**`.github/workflows/ios.yml` に GitHub Actions の
ワークフローを用意した(公開リポジトリなので macOS ランナーは無料)。

使い方:
1. App Store Connect → ユーザとアクセス → 統合 → App Store Connect API で
   チームキーを発行(ロールは Admin。.p8 は一度しか落とせないので保管する)
2. リポジトリの Settings → Secrets and variables → Actions に3つ登録:
   `ASC_KEY_ID` / `ASC_ISSUER_ID` / `ASC_KEY_P8`(.p8 の中身をそのまま貼る)
3. Actions タブ → 「iOS build & upload」→ Run workflow

署名は Xcode のクラウド管理。証明書もプロファイルも CI が API キーで自動生成する。
ビルド番号は GitHub の実行番号なので衝突しない。アイコンは CI 内で
雛形のプレースホルダから `assets/appicon/icon-1024.png` に差し替える。

- 実機テストは TestFlight。**TestFlight の課金は自動的に Sandbox 扱い**になるので、
  審査で必ず見られる「削除→再インストール→購入を復元」もここで通せる
- App Store Connect 側は設定済み:App記録(4+・教育・日本のみ・本体無料)、
  プライバシー(収集なし)、課金アイテム(¥680・審査用スクショ・提出下書きに追加済み)
- 商品ページ用スクリーンショット5枚(1320×2868)は生成済み(著者の手元に保存)
- **組織移行を申請すると証明書ポータルが一時使えなくなる。**初回ビルドと署名は
  移行申請の前に済ませておくこと(いまの順番で正しい)

---

## 済んでいる手続き

- Apple Developer Program 登録済み
- App Store Connect 利用規約に同意済み
- 法人情報:`Kazuhiro Hashino`(ローマ字)
- **有料アプリ契約:有効**
- 銀行口座:楽天銀行(3770)登録済み
- 税務フォーム:2種とも**有効**(W-8BEN は第12条・0%)
- 小規模事業者プログラム:申請済み(承認待ち)

手数料は承認後、**承認された会計月の月末から15日後**に 30% → 15% になる。
さかのぼっての適用はない。

---

## 決めていないこと

- EU での配信(有効にすると DSA により**氏名と住所が公開**される。当面は対象外にする)
- 商標出願(事業が形になってから)
- note 連載の第4回以降(`note_claude` リポジトリ側の作業)

---

## 二度と変えられない項目(登録前に必ず確認する)

Apple 公式の
[編集可否の一覧](https://developer.apple.com/help/app-store-connect/reference/app-information/required-localizable-and-editable-properties)
で確認した内容。**ここに挙げた値は、登録した瞬間に確定して戻せない。**

### 完全に変更不可(アプリを消しても同じ値を再利用できない)

| 項目 | 決めた値 | 補足 |
|---|---|---|
| バンドルID | `com.bridgeaitokyo.toeic` | 所有ドメインの逆順。ハイフン無しにしてあるのは、Android のアプリIDが Java のパッケージ名規則に従い**ハイフンを使えない**ため |
| SKU | `toeic-workbook-001` | 非公開の管理用ID。アプリを削除しても再利用できない。次の製品は `<製品名>-workbook-002` のように続ける |
| 課金アイテムの製品ID | `com.bridgeaitokyo.toeic.unlockall` | **削除しても同じIDを二度と使えない**(Apple 公式回答) |
| 課金アイテムの種類 | 非消費型 | 作成後に種別を変えられない |

将来アプリを増やすときは `com.bridgeaitokyo.<製品名>` で揃える。
Apple の管理画面で並んで見え、100個規模になっても破綻しない。

### バージョン更新でしか変えられない(その場では直せない)

- アプリ名 / サブタイトル
- プライマリ言語(日本語で確定)
- プライマリ・セカンダリカテゴリ
- Content Rights(第三者コンテンツの有無)

### いつでも変えられる

価格、各種URL、年齢制限、プライバシー申告、配信する国と地域、著作権表記。

**ここが値上げの自由度になる。**価格はいつでも変えられるので、
最初から高く付ける必要はない。

## Apple の管理画面が固まるとき

Chrome の拡張機能(広告ブロッカー・プライバシー系)が Apple のスクリプトを
止めることがある。`Register an App ID` の途中で読み込み中のまま固まる症状が出た。

**Microsoft Edge で開くと通る。**Apple 関連の作業は Edge で行うのが確実。

---

## 価格の決定(2026年8月・確定 680円)

候補 380/500/680/780/880/980/1,980 を比較して 680円 に確定した。判断の骨子:

- 日本のTOEICアプリの価格の錨は「金のフレーズ」(650円)。その帯なら
  「定番と同じ価格で模試10回分」という比較が成立する
- 1,000円の壁の内側に置き、AI製であることへの期待値を上げすぎない。
  価格は品質の期待値を設定する。高いほどレビューの採点が辛くなり、
  低いほど「この値段でこの量なら」と甘くつく。レビューは複利で効く唯一の資産
- 380円は捨てた。無料セット1で試してから買う構造なので、買い手は必ず吟味する。
  吟味して買う商品に衝動買い価格を付けても購入率は伸びず、単価だけ失う
- 780/880円は捨てた。錨(650円)の上・壁(980円)の下で記号性がない死に値

**見直し規則:**ストア閲覧500件時点で購入率2%未満なら要因分析。
評価4.3以上でレビューが数十件になったら980円への引き上げを検討。
値上げ・値下げとも Apple の制限はなく、審査不要・即日反映・既購入者に影響なし。

## 撤退プラン(先に決めてある)

- **売れない** → 撤退不要。維持費ゼロ(年会費は全アプリ共用)。置いたまま診断データを貯める
- **星1が付く** → ①全レビューに開発者返信 ②中身を直してバージョン更新時に
  評価サマリーをリセット(Apple公式機能) ③最終手段:販売停止し、新バンドルID
  (例 com.bridgeaitokyo.toeic2)で別アプリとして再投入。レビューはゼロから始まる。
  一度公開したアプリの完全削除は制限が多く、実務上は販売停止=撤退
- **安すぎた** → 最も安い失敗。いつでも値上げでき、既購入者は何も失わない。
  取り返しがつかないのは高すぎ(星1の複利)側で、安すぎ側は完全に可逆
