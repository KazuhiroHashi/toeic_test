# note で売る手順

アプリは1つのURLで公開し、**セット1は無料・セット2〜10は合言葉で解放**する仕組みになっています。
note の有料記事に合言葉を書いて売ります。決済も入金も note がやってくれるので、あなたが用意するのは記事だけです。

商品は2ラインあります。

1. **標準(セット2〜6)** — 公式問題集〜700点クラス中心。既存の5セット。
2. **上級(セット7〜10)** — 800〜900点を狙う人向け。やさしい問題を入れず、関係詞・構造・文書の突き合わせで難度を上げた4セット。

## 合言葉と価格の一覧

**価格は 680円・全部入り1本だけで始める。**iOSアプリ版(セット2〜10の一括解放)と同額。
まとめ・単品の細かい価格段階は、最初は出さない(選択肢が多いほど買わない理由が増える)。
合言葉は用意済みなので、将来キャンペーンや単品売りを試すときに記事を足すだけでよい。

### いま売るもの

| 商品 | 合言葉 | 価格 |
|---|---|---|
| **全部入り(セット2〜10・1,800問)** | `harbor-lantern` | **680円** |

680円の根拠:日本のTOEICアプリの価格の錨は「金のフレーズ」(物書堂・650円)。
その帯に置けば「定番アプリと同じ価格で模試10回分」という比較がそのまま武器になる。
1,000円の壁の内側で、期待値を上げすぎない(AI製であることに対しレビューが甘くつく)。

**値上げはいつでもできる(Apple・note とも制限なし)。**評価が安定して
「安すぎる」の声が出たら 980円 への引き上げを検討する。値下げも自由。
買い切り型なので、どちらに動かしても既購入者は何も失わない。

### 予備の合言葉(いまは記事に載せない)

| 商品 | 合言葉 |
|---|---|
| 標準まとめ(セット2〜6) | `copper-window` |
| 上級まとめ(セット7〜10) | `velvet-anchor` |
| セット2〜10 単品 | `maple-compass` / `amber-signal` / `willow-marble` / `crimson-ferry` / `olive-tunnel` / `cedar-beacon` / `indigo-basket` / `silver-pillow` / `golden-thunder` |

- 大文字小文字・前後の空白は自動で吸収されます(`  HARBOR-LANTERN ` でも通ります)
- 合言葉そのものはアプリのソースに書かれていません。照合用の値だけが入っています

## 手順

### 1. アプリを公開する(すでに公開済み)

https://toeic.bridge-ai-tokyo.com/

#### 記事を書いたら、購入ページのURLをアプリに登録する

アプリのホームには「セット1は無料 / セット2以降は有料」と出ますが、
**買える場所へのリンクは、URLを登録するまで出ません**(リンク切れを出さないため)。

note の記事を公開したら、`js/app.js` の次の行に記事のURLを入れてください。

```js
var SHOP_URL = "";   // ← ここに note の記事URLを入れる
```

例:

```js
var SHOP_URL = "https://note.com/あなたのID/n/xxxxxxxx";
```

入れると、ホームの説明の下と、合言葉ボタンの横に『セットを購入する →』が出ます。
**これが無いと、買いたい人が買い方にたどり着けません。**忘れずに登録してください。

> アプリ版(App Store)ではこのリンクは自動的に出ません。
> ガイドライン 3.1.1 で、アプリ内の機能解放を外部の購入手段へ誘導することが
> 禁止されているためです(出すとリジェクトされます)。

### 2. note で記事を作る

**まずは『全部入り』の1本だけ書いてください。**単品やまとめの記事は、
売れ始めてから間口を広げるために足すもので、最初に11本そろえる必要はありません。

**無料部分に書くこと**
- どんな問題集か(本番と同じ200問構成、全問に日本語解説と全訳、リスニング音声つき)
- 画面のスクリーンショット
- **「セット1は無料で全部解けます」+ アプリのURL** ← これが一番効きます
- あなたの実績(独学3年で資格30以上、TOEIC 950・R満点)

**有料部分に書くこと**
- アプリのURL(もう一度)
- 合言葉
- 使い方3行(URLを開く → セット2のタブを押す → 合言葉を入れる)

### 3. 価格(上の一覧のとおり)

note の手数料は売上の15%前後(プラットフォーム利用料+決済手数料)。
680円なら手取りは約578円です。

**まず『全部入り 680円』の1本から始めてください。**11本すべてを最初に書く必要は
ありません。単品やまとめは、全部入りが売れ始めてから、間口を広げるために足します。

値上げ・値下げとも自由(買い切り型なので既購入者は何も失わない)。迷ったら安いほうから始めてください。

### 4. 売ったあとも改良できます

**購入者は次にURLを開いた瞬間、最新版になります。**買い直しは不要です。
「購入後の改良もすべて無料で反映されます」と書けるので、これは売り文句になります。

## 合言葉を変えたいとき

```bash
node tools/make_code.js 好きな合言葉
```

出てきた値を `js/app.js` の `CODE_HASHES` に貼り、対応するセットidを書きます。

```js
var CODE_HASHES = {
  "出てきた値": ["s2"],
  ...
};
```

## 無料で公開するセットを変えたいとき

`js/app.js` の次の行を書き換えます。

```js
var FREE_SETS = { s1: true };        // セット1だけ無料
var FREE_SETS = { s1: true, s2: true };  // セット2も無料にする場合
```

## 正直な限界

- 合言葉を人に教えられたら、その人も使えます。ブラウザの開発者ツールを開けば問題文も見えます
- これは note の有料記事がコピペできるのと同じ話です。680円の商品にサーバーとログインを用意するのは費用対効果が合いません
- 合言葉の照合値はソースに入っているので、その気になれば総当たりで割り出せます。ここは強度ではなく手間の問題として割り切っています
- 売れ行きが大きくなってから、サーバー方式に移行すれば十分です
